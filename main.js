import prompt from "prompt-sync";

const gameState = {
    isPlaying: true,
    rounds: 0,
};

while (gameState.isPlaying) {
    // check gameState and show output
    // calculate which input is allowd (menu)

    // get input 

    // go to next round
}